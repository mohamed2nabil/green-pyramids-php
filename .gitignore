# Google Service Account credentials
admin/scratch/*.json
secure_credentials/
google-service-account*.json

# Dependencies
/vendor/
/composer.lock

# Runtime analytics cache (never commit report data)
/admin/cache/analytics/

error_log

# Ignore image asset folders (do not upload images to the repo)
images/
assets/images/
